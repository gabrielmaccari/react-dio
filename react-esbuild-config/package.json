{
  "name": "react-esbuild-config",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "esbuild src/index.js --outfile=dist/bundle.js --loader:.js=jsx --bundle --minify --serve --servedir=dist --watch",
    "build": "esbuild src/index.js --outfile=dist/bundle.js --loader:.js=jsx --bundle --minify",  
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
